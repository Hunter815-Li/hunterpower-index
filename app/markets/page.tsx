import type { Metadata } from "next";
import { MarketBoard } from "@/components/market/MarketBoard";
import { MarketComparison } from "@/components/market/MarketComparison";
import { BilingualText } from "@/components/ui/BilingualText";
import { getMarketBoard } from "@/lib/market-data/market-board";

export const metadata: Metadata = { title: "全球市场终端", description: "全球股指、美债收益率、外汇、商品、加密资产及波动率真实次日日线总览。" }; export const dynamic = "force-dynamic";
export default async function MarketsPage() { const data = await getMarketBoard(); const groups = Array.from(new Set(data.map((item) => item.group))); return <main className="markets-page terminal-section"><section className="shell terminal-hero"><span className="eyebrow">GLOBAL MARKET TERMINAL</span><h1><BilingualText zh="全球市场总览" en="Global Markets" /></h1><p><BilingualText zh="真实跨资产日线、风险指标与相对表现。统一按已完成交易日更新，不使用合成行情。" en="Real cross-asset daily data, risk indicators and relative performance, updated after completed trading days without synthetic quotes." /></p><div className="terminal-clock">T+1 EOD <b>DAILY REFRESH</b><span>MARKET DATA · FRED</span></div></section><div className="shell markets-stack"><MarketBoard data={data} />{groups.map((group) => <section className="terminal-panel asset-table" key={group}><div className="terminal-panel-head"><h2>{group}</h2><span>{data.filter((item) => item.group === group).length} INSTRUMENTS</span></div>{data.filter((item) => item.group === group).map((item) => <div className="asset-row" key={item.key}><b>{item.name}<small>{item.symbol} · {item.source ?? "Source unavailable"}</small></b><strong>{item.price === null ? "—" : item.price.toLocaleString()}</strong><span className={item.changePercent === null ? "muted" : item.changePercent >= 0 ? "positive" : "negative"}>{item.changeLabel ?? "Data unavailable"}</span><time>{item.updatedAt?.slice(0, 16).replace("T", " ") ?? "—"}</time><em>{item.marketStatus === "delayed" ? "T+1 EOD" : "unavailable"}</em></div>)}</section>)}<MarketComparison /></div></main>; }
