import type { Metadata } from "next";
import Link from "next/link";
import { hunterPowerIndex } from "@/data/indices/hunter-power";
export const metadata: Metadata = { title: "Hunter Power Index 方法论", description: "HPI 样本空间、等权计算、再平衡与数据处理规则。" };
export default function MethodologyPage() { return <main className="reading-page methodology-page"><article className="article-shell"><header><Link className="back-link" href="/indices/hunter-power">← 返回指数</Link><span className="eyebrow dark">METHODOLOGY · VERSION 1.0</span><h1>Hunter Power Index<br />指数方法论</h1><p className="article-subtitle">透明说明指数目标、样本、计算方法与局限。</p></header><div className="methodology-body"><section><span>01</span><div><h2>指数目标</h2><p>衡量美国上市的电力设备、电网建设、数据中心电力、核能与发电、公用事业、先进核能及储能产业链代表性公司的整体市场表现。</p></div></section><section><span>02</span><div><h2>基础参数</h2><dl>{[["Index Name", hunterPowerIndex.name], ["Ticker", hunterPowerIndex.ticker], ["Base Value", String(hunterPowerIndex.baseValue)], ["Currency", hunterPowerIndex.currency], ["Weighting", hunterPowerIndex.weighting], ["Rebalance", hunterPowerIndex.rebalance]].map(([key, value]) => <div key={key}><dt>{key}</dt><dd>{value}</dd></div>)}</dl></div></section><section><span>03</span><div><h2>计算方法</h2><p>每只成分股使用复权收盘价。先以各自基准日价格标准化为 100，再对同一交易日的标准化价格做等权算术平均，不直接平均股票涨跌幅。</p><pre>HPI(t) = (1 / N) × Σ [P(i,t) / P(i,0) × 100]</pre><p>缺失交易日使用最近一个有效复权价格向前填充；上市前不回填。有效历史不足的证券会被排除，并在页面披露警告。</p></div></section><section><span>04</span><div><h2>再平衡与维护</h2><p>计划按季度审核样本和恢复等权。首次正式基准日、纳入/剔除缓冲规则及公司行动细则将在获得完整可授权历史数据后锁定版本，不做事后伪造。</p></div></section><section><span>05</span><div><h2>数据与局限</h2><p>数据依所配置的服务端供应商而定，可能存在延迟、缺失或修订。所有指数及统计计算在服务端完成；数据不可用时显示明确缺省状态。</p></div></section></div></article></main>; }
