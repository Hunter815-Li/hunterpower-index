# Research MDX content

Only verified, publishable research belongs here. Add one `.mdx` file per article. Required frontmatter:

`title`, `subtitle`, `date`, `category`, `tags`, `summary`, `coverImage`, `wechatUrl`, `featured`, `readingTime`.

Optional: `updatedAt`, `dataSources`. No sample article is included because the production site must not publish fabricated research.
